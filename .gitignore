# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
ENV/
env/

# PyCharm
.idea/

# VS Code
.vscode/

# Jupyter Notebook
.ipynb_checkpoints
notebooks/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Data directory (never commit raw data)
# No trailing slash — also matches the symlink at the project root that points to /mnt/d/...
/data
/data/

# Training outputs (at project root, not source code)
# These may be symlinks to /mnt/d/...; no trailing slash matches the symlink too
/runs
/runs/
/inference
/inference/
/metrics/

# Curated/blessed model artifacts (large checkpoints; backed up via SSD, not git)
/models/

# Per-machine setup notes (machine-specific, not for the repo)
LOCAL_SETUP.md

# Size analysis artifacts (large prediction outputs, local only)
/scripts/_size_analysis_preds/
/scripts/_size_analysis_results.csv

# Model checkpoints (too large for git)
*.pth
*.pt
*.ckpt
*.onnx

# TensorBoard logs
events.out.tfevents.*

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Temporary files
*.swp
*.swo
*~
.*.sw?

# Windows Zone.Identifier files
*:Zone.Identifier

# PyInstaller build outputs
/dist/
/build/
*.spec.bak

# PyTorch extensions
*.so
*.pyd
